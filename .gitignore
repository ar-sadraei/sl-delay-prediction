# Environments
.venv/
__pycache__/
*.pyc

# Secrets — never commit API keys
.env

# Data — never commit raw or processed data, only the code that generates it
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# OS/editor noise
.DS_Store
.ipynb_checkpoints/